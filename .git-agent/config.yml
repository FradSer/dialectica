scopes:
    - name: dialectica
      description: Core engine, agent protocols, and processing stages in dialectica/; does not cover test suites or project-wide configuration
    - name: tests
      description: Automated verification and integration test suites in tests/; does not cover application source code or build configuration
